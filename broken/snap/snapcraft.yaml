name: rollback-demo-kyrofa
version: 0.1.1
grade: devel
confinement: strict
summary: Rollback Demo
description: |
  Demonstration of rollbacks using the configure hook as a health check.

architectures: [all]

apps:
  test:
    command: echo "I\'m the broken version"

parts:
  my-part:
    plugin: nil
